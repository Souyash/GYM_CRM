# Optional for local dev (defaults to local proxy /api)
# For production Vercel deployment, set this to your hosted backend URL (e.g. https://your-backend.onrender.com)
VITE_API_URL=
VITE_SOCKET_URL=
